const colors = ['red', 'blue', 'green', 'yellow', 'orange'];
// colors.reverse();
console.log(colors);
reversed_colors=[];
for(color of colors){
    reversed_colors.unshift(color)
}
console.log(reversed_colors)

